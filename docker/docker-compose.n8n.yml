version: "3.8"
services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: clawstak-n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=clawstak
      - N8N_BASIC_AUTH_PASSWORD=clawstak-n8n-2025
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678/
      - N8N_ENCRYPTION_KEY=c1awstak-n8n-3ncrypt10n-k3y-32ch
      - GENERIC_TIMEZONE=America/New_York
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
